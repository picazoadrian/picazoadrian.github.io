# Changelog

## [Sin publicar]

### Añadido
- Andamiaje del sitio: estructura HTML, hoja de estilos con tokens, datos de proyectos
  en `projects.js` y capa de comportamiento en `main.js` (hover, toggle táctil, lightbox,
  carga diferida de vídeo, scroll suave).
- `scripts/encode.sh`: pipeline de vídeo VP9 1080p + fallback H.264 + poster WebP.
- Placeholders grises numerados para la primera versión, a la espera del material real.
